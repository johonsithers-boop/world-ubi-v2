{
  "name": "world-ubi-contracts",
  "version": "1.0.0",
  "description": "World UBI Coin smart contracts for World Chain",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network worldChainSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network worldChain",
    "verify": "hardhat verify",
    "clean": "hardhat clean",
    "size": "hardhat size-contracts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  }
}
